const initState = {
  loadingShow: false,
  itemList: [],
  collection: [],
  AccessoriesReducer: 0
}

export default function HomePageReducer(state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}